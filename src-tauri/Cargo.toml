[package]
name = "voice-forge-portable"
version = "1.0.0"
description = "Lanceur Windows portable et hors ligne pour Voice Forge"
authors = ["Voice Forge"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }
ico = "0.5"
png = "0.17"

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
